name: Build & Release HireMonkey

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [intel, arm64]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build for macOS ${{ matrix.arch }}
        run: npm run build-mac-${{ matrix.arch }}

      - name: Create DMG
        run: npm run dmg-${{ matrix.arch }}

      - name: Upload DMG to artifacts
        uses: actions/upload-artifact@v4
        with:
          name: hiremonkey-mac-${{ matrix.arch }}
          path: builds/hiremonkey-darwin-${{ matrix.arch }}/*.dmg

      - name: Upload DMG to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          files: builds/hiremonkey-darwin-${{ matrix.arch }}/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build for Windows
        run: npm run build-win

      - name: Zip Windows build
        run: Compress-Archive -Path builds\HireMonkey-win32-x64\* -DestinationPath builds\HireMonkey-win32-x64.zip

      - name: Upload ZIP to artifacts
        uses: actions/upload-artifact@v4
        with:
          name: hiremonkey-win
          path: builds/HireMonkey-win32-x64.zip

      - name: Upload ZIP to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          files: builds/HireMonkey-win32-x64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
